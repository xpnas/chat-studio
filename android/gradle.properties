org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental compilation cannot relativize Pub sources on C: to builds on D:.
# Disabled for reproducible Windows builds; Dart hot reload is unaffected.
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process

# Keep native compilation within local/CI memory limits.
org.gradle.workers.max=2
